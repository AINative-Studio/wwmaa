# NPM Configuration for Railway Deployment
# Ensures clean installs and prevents build cache issues

# Use exact versions from package-lock.json
package-lock=true

# Enable strict SSL for security
strict-ssl=true

# Skip optional dependencies to reduce build time
optional=false

# Update browserslist database automatically
update-browserslist-db=true
